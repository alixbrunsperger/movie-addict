import React from 'react';

export default ({showPopup}) => (
  <div className="search">
      <div className="search-button" onClick={showPopup}>
        <img src="/images/finder.svg" alt="icon finder" />
      </div>
    </div>
  );
  
